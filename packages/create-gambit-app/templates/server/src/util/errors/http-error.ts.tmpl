/**
 * Re-exported from `@guisao-llc/gambit-auth` so the familiar import path keeps
 * working. Subclass it here if your app needs to carry extra context on an
 * error — one app adds a `details` field for refusals a client must act on.
 */
export { HttpError } from "@guisao-llc/gambit-auth";
export { HttpError as default } from "@guisao-llc/gambit-auth";
